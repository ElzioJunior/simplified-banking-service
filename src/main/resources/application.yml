spring:
  application:
    name: simplified-banking-service
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/simplified_banking}
    username: ${DATABASE_USERNAME:simplified_banking}
    password: ${DATABASE_PASSWORD:simplified_banking}
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      jakarta.persistence.lock.timeout: ${TRANSFER_LOCK_TIMEOUT_MS:5000}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:simplified_banking}
    password: ${RABBITMQ_PASSWORD:simplified_banking}
    publisher-confirm-type: correlated

transfer:
  notifications:
    publisher:
      enabled: ${TRANSFER_NOTIFICATION_PUBLISHER_ENABLED:true}
      batch-size: ${TRANSFER_NOTIFICATION_BATCH_SIZE:50}
      fixed-delay-ms: ${TRANSFER_NOTIFICATION_RETRY_DELAY_MS:1000}
      confirm-timeout-ms: ${TRANSFER_NOTIFICATION_CONFIRM_TIMEOUT_MS:5000}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: never

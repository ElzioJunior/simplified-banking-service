# Database Documentation

Keep the logical data model and data ownership rules here. The logical model
documents intent; versioned migrations are the source of truth for the physical
schema once implementation exists.

Use [the logical data-model template](logical-data-model-template.md) when the
project persists application data.
